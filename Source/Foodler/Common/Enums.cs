using System.ComponentModel;

namespace Foodler.Common
{
    [DefaultValue(None)]
    public enum MainPivotPage
    {
        None = -1,
        Participants = 0,
        Food = 1,
        Sum = 2,
    }
}
